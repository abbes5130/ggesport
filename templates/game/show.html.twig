{% extends 'admin/index.html.twig' %}

{% block title %}Game{% endblock %}

{% block content %}
    <h1>Game</h1>

    <table class="table" >
        <tbody>
            <tr>
                <th>IdGame</th>
                <td>{{ game.idGame }}</td>
            </tr>
            <tr>
                <th>GameName</th>
                <td>{{ game.gameName }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ game.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_game_index') }}">back to list</a>

    <a href="{{ path('app_game_edit', {'idGame': game.idGame}) }}">edit</a>

    {{ include('game/_delete_form.html.twig') }}
{% endblock %}
