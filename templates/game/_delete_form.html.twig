<form method="post" action="{{ path('app_game_delete', {'idGame': game.idGame}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game.idGame) }}">
    <button class="btn">Delete</button>
</form>
