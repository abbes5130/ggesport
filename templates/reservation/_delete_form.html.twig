<form method="post" action="{{ path('app_reservation_delete', {'idTicket': reservation.idTicket}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.idTicket) }}">
    <button class="btn">Delete</button>
</form>
