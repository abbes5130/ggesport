<form method="post" action="{{ path('app_match_team_delete', {'idMatchTeam': match_team.idMatchTeam}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match_team.idMatchTeam) }}">
    <button class="btn">Delete</button>
</form>
