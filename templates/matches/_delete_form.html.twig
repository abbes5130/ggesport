<form method="post" action="{{ path('app_matches_delete', {'idMatch': match.idMatch}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match.idMatch) }}">
    <button class="btn">Delete</button>
</form>
