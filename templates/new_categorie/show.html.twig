{% extends 'admin/index.html.twig' %}

{% block title %}Newcategorie{% endblock %}

{% block content %}
    <h1>Newcategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newcategorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ newcategorie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_new_categorie_index') }}">back to list</a>

    <a href="{{ path('app_new_categorie_edit', {'id': newcategorie.id}) }}">edit</a>

    {{ include('new_categorie/_delete_form.html.twig') }}
{% endblock %}
