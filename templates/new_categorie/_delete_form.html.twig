<form method="post" action="{{ path('app_new_categorie_delete', {'id': newcategorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newcategorie.id) }}">
    <button class="btn">Delete</button>
</form>
