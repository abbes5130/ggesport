<form method="post" action="{{ path('app_player_delete', {'idPlayer': player.idPlayer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player.idPlayer) }}">
    <button class="btn">Delete</button>
</form>
