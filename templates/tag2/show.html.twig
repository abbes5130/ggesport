{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ tag.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tag2_index') }}">back to list</a>

    <a href="{{ path('app_tag2_edit', {'id': tag.id}) }}">edit</a>

    {{ include('tag2/_delete_form.html.twig') }}
{% endblock %}
